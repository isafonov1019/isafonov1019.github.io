// @ts-check
import { defineConfig } from 'astro/config';

// Настройки Astro. Поле `site` понадобится на этапе публикации —
// туда впишем реальный адрес сайта на Netlify.
export default defineConfig({
  site: 'https://example.netlify.app',
});
